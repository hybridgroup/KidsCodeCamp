.rules
  %h3 Forum Rules
  %p Please read forum rules before you post.

= render :partial => "layouts/user_controls"

.clear

= form_tag("/search", :method => "get", :class => "search-forum") do
  = text_field_tag :search, params[:search], :class => "search-text"
  = image_submit_tag("magnifying-glass2.png", :class => "search-image")
.clear